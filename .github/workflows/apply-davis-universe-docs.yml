name: apply-davis-universe-docs

on:
  push:
    branches: [main]
    paths:
      - 'scripts/apply-davis-universe-docs.mjs'
      - '.github/workflows/apply-davis-universe-docs.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/apply-davis-universe-docs.mjs
      - name: Commit documentation updates
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add README.md experiments/davis-2026-affix-replication.md
          if git diff --cached --quiet; then
            echo 'No documentation changes needed.'
          else
            git commit -m 'Document Davis 50-sign universe clarification'
            git push origin HEAD:main
          fi
